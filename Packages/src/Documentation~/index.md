# Oloc

`CountryFlagCollection`은 국가별 국기 `SpriteAtlas`와 직렬화된 `Sprite[]`를 함께 보관합니다.

1. `Create > Oojjrs > Oloc > Country Flag Collection`에서 에셋을 만듭니다.
2. `Atlas`에 국기 Sprite Atlas를 지정합니다.
3. `Sprites`에 같은 국기 Sprite를 필요한 순서대로 지정합니다.

이미지와 Atlas 에셋은 패키지에 포함하지 않습니다.
